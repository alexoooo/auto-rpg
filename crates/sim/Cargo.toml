[package]
name = "sim"
description = "Deterministic headless simulation core -- no engine, no renderer, no floats"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
lab-calibration = []
cartesian-recoil = []

[dependencies]
fx = { path = "../fx" }
