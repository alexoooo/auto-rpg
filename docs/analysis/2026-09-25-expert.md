# The reference expert (skill ceiling session 04)

Plan: `docs/plans/2026-09-25-skill-ceiling-04-expert.md`. Code: `tests/harness/expert.mjs`, its test
`tests/expert.test.mjs`, the drill runner's planner hook and `DRILL_TASKS` in
`tests/harness/drills.mjs`, and the `expert...` corners of `research/drills.mjs` and
`research/league.mjs`. The expert never ships: it lives under `tests/harness/`, beside the fork it
searches on, and no page imports it.

## Answer

- **The gate passes.** On x1 stone, the full-knowledge expert beats the duelist clearly in drills
  and in the league. Its default is 8 candidates, a 1 s horizon and 4 decisions a second
  (`expert@c8,h1`).
  - **League, n = 128.** The harness is the Node bout runner through `research/league.mjs`: blade
    class, `default` against its four body variants, 8 clusters. The expert won **128 of 128**,
    128/128 on each side. Its bar margin was 0.833 [0.812, 0.853] and the margin's d was 8.37. The
    bouts lasted a mean 5.2 s and the winner kept 0.861 of its bar.
  - **Drills.** The harness is the drill runner in `research/drills.mjs`, with every rung paired by
    start. On every drill the expert passes at least as often as the duelist. It is clearly ahead
    on four drills, by pass rate or by the drill's margin, and level on finish:

    | drill | n | duelist pass | expert pass | expert − duelist | margin, duelist → expert |
    |---|---:|---:|---:|---:|---|
    | survive-cut | 93 | 32.3 % | 86.0 % | +53.8 ± 10.6 | wound 0.051 → 0.013 |
    | punish-miss | 200 | 67.5 % | 100 % | +32.5 ± 6.5 | first blow 0.50 → 0.26 s |
    | land-clean-blow | 200 | 79.0 % | 100 % | +21.0 ± 5.7 | clean blow 0.55 → 0.21 s |
    | get-inside | 200 | 91.5 % | 100 % | +8.5 ± 3.9 | time inside 1.21 → 0.39 s (+0.82 ± 0.09) |
    | hold-range | 200 | 96.5 % | 99.5 % | +3.0 ± 2.7 | share in band 0.982 → 0.996 (+0.014 ± 0.004) |
    | finish | 200 | 100 % | 100 % | 0 | first blow 0.096 → 0.103 s (−0.007 ± 0.007) |

    survive-cut's n counts scored starts; 107 were void.

    Two of the time margins are not wins over the whole ladder:
    - On get-inside the walker is faster than the expert, 0.33 against 0.39 s (−0.059 ± 0.022).
    - On finish the expert is level with the duelist at c8, and slower at c4 (0.132 s).

    The cause is the objective, not the search: in a three-second drill it prices a second of
    earliness at 0.25 / 3 bars, beside the bout's damage at full weight (§3, §4).

- **The sanity checks.**
  - **It beats idle on every body it was put on.** In the league, all seven weapon classes against
    every variant: **108 of 108** bouts, both sides, with a pooled margin of 0.856. On the odd
    morphology (`the_expert_beats_idle_on_the_odd_pair`), it empties the idle body's bar and loses
    none of its own.
  - **It does not lose its own mirror by side.** The left corner scored:
    - 50.0 % over 32 distinct blade bouts;
    - 46.9 % over 32 distinct two-blades bouts;
    - 48.4 % pooled over 64, against a band of ±12.3.
  - **A blinded fork makes it measurably worse,** but only when the blinding is timed to matter.
    That is a finding about the mutation (§5).
    - `-lag` plans every search on the capture one decision old, and does not search at the first
      decision. On survive-cut it falls from 86.0 % to 29.0 %, the duelist's level.
    - `-blind` plans on a capture four decisions old. Until four decisions have passed, it uses the
      oldest capture it holds, so in a one-second rung the first decision is exact. It is level
      with the full expert on survive-cut, where that first decision is the parry. It falls to
      59.0 % on punish-miss (from 100 %) and to 0.52 s from 0.21 s on land-clean-blow's time to a
      clean blow.
- **Score against compute: the drill pass rates are flat in candidates from c4.** Finish's time to
  first blow flattens at c8, so headroom is called at c8 on what the drills measure.
  - **The search's own objective is not flat.** On get-inside it keeps climbing to c32. Only c32's
    first round reaches the back-off-then-cut proposal, the last on the list, and it trades an
    early entry for a blow with it (predicted damage 0.111 against c8's 0.046). It gets inside at
    0.84 s against c8's 0.39 (−0.45 ± 0.10), still passing every start. More search there serves
    the objective better and the drill's margin worse. It also shows that candidates buy proposal
    coverage, not only density (§3).
  - **Horizon is not a budget axis here but a trade-off.** On the three-second drills a shorter
    horizon is faster to a pass: get-inside 0.33 / 0.39 / 0.40 s at h0.5 / h1 / h1.5.
  - **Survive-cut's low line** (15 scored starts) is still rising and is reported as a lower bound:
    20 → 27 → 33 → 33 % from c4 to c32 (§3).
- **Model-only beside full knowledge.** With the persistence model in the opponent's place:
  - on survive-cut the expert falls from 86.0 % to 50.5 %;
  - on punish-miss, land-clean-blow, get-inside, hold-range and finish it is level with full
    knowledge, within a few points;
  - in the league, n = 64, it still beats the duelist, 85.9 % [78.1, 93.8] with d 1.51.

  So the expert wins the league on search alone, but reading the duelist more than doubles its bar
  margin (0.389 → 0.833). Survive-cut's headroom is mostly reading: the parry has to be chosen
  before the cut shows.
- **Cost.** Node bout runner, default against default, one lane: **2.7 s a decision** at c8, h1. That
  is 11 s of wall a game second, 1/11 of real time. A 1 s rollout is 47 ms of restore and 281 ms of
  simulation, and a capture is 37 ms a decision. On a host running 16 to 20 lanes it is 3.1 s a
  decision and 12.6 s a game second (league, n = 2,743 decisions). The cost scales with candidates:
  c32 is about 4× c8.

## 1. Design

A **receding-horizon sampling search over plans, on exact forks of the live world.** Every
`1 / decisionHz` seconds (4 Hz by default) the expert does the following:

1. It captures the world with Havok's heap (`captureBout(bout, { heap: true })`).
2. It scores each candidate plan by rolling the plan out for `horizon` seconds in an exact fork.
3. It commits the best plan and plays it until the next decision.

**A candidate is a plan, not a command list.** A plan has one or two segments and a switch time.
Each segment is a small closed-loop controller over the published view:

- **`follow`**: a naive-ladder mind's continuation, with forward and strafe offsets;
- **`hold`**: the last command, held;
- **`stance`**: footwork, pointing at their shoulder or covering a line;
- **`stroke`**: one stroke at a sampled mark, lifted and moved sideways, then a covering stance.

Every segment reads the view every substep. So the prefix the expert commits to reproduces the
rollout's commands exactly whenever the world does. That is what
`a_rollout_is_the_future_it_predicts_in_a_bout` holds, bit for bit, on the odd morphology.

**Proposals.** Round 1 is a fixed list in priority order, and round 2 jitters the best few.

- The fixed list: the duelist's own continuation, the plan in progress carried warm, cut-mid,
  step-in-and-cut, back off, cut-high, press, the walker, guard, cut-then-back-off, circle left and
  right, cut-low, hold, duelist-then-cut, and back-off-then-cut.
- Round 1 takes the first `candidates − ⌊candidates / 3⌋` plans of the list, filling any room
  left with random plans from its families, and round 2 is the other third. So at c8 round 1 is
  the first six plans (the warm plan counts as one when there is one), c16 the first eleven, and
  only c32 reaches the whole list.
- A decision on which every candidate ties is the duelist's, because ties go to the earlier
  candidate.
- The search does not care where a plan came from.

**The command surface is `Intent`**, the controller a person uses, as the owner decided. The
segments write it through the same tactics helpers the naive minds use (`aimAt`, `writeAim`,
`strokeCommand`, `pointAt`, `restHand`). Nothing reaches past `Intent`.

**The objective** is `scoreRollout`, in bars, with every term logged per decision:

| term | weight | what |
|---|---:|---|
| damage | 1 | bar dealt minus bar taken over the horizon |
| end | 1 | +1 / −1 if the bout is decided inside it |
| down | 0.08 | each body's terminal support state (fallen 1, rising 0.5), theirs minus mine |
| position | 0.03 | how far my striker's range fraction ends outside [0.65, 1.0] |
| stall | 0.03 / s | session 03's near-range stall seconds, accrued by me |
| retreat | 0.03 / s | session 03's retreat-outside-reach seconds, accrued by me |
| task (drills only) | 1 | the drill's own judge over the rollout (`DRILL_TASKS`) |

- **Stalling is priced**, as the owner asked. Across the league gate the expert's stall and retreat
  columns read 0.00 s a bout, against the duelist's 0.00 too.
- **In a drill** the rollout stops where the rung does, and the drill's judge scores it in the fork:
  - a pass is worth 1;
  - the drill's margin shapes the score on either side of a pass: a wound, a time to pass, the
    share in band.

  `a_rollout_is_the_future_it_predicts_in_a_drill_rung` checks every candidate's task term against
  survive-cut's pass less the rollout's own wound.
- **`-bout`** plays a drill on the bout objective alone. It is the control for the task term.
  - It is level with the task objective on punish-miss and land-clean-blow.
  - It is 30 points worse on survive-cut, because a wound under 0.03 is not what the bout
    objective prices.
  - It holds range in **1.0 %** of hold-range starts (n 200), because the bout objective's 0.03 a
    fraction for position is outweighed by a blow.
  - It falls below the duelist on get-inside's pass rate (90.5 %) and takes 0.97 s to get inside.

**The two instruments**, as the owner decided, full knowledge first:

- **Full knowledge.** Inside a rollout the opponent is its real mind. The fork restores it from the
  capture, and its random streams are reseeded once per decision, so every candidate is compared on
  common random numbers. The expert knows the policy and not the dice.
  - The duelist draws its dice only on a few state changes. Over a 1 s rollout it barely draws, so
    against the duelist full knowledge is close to perfect prediction.
  - That is why the prediction test's control is the persistence model and not restarted dice.
    With the dice restarted, every prediction still came true.
- **Model-only (persistence).** The opponent in a rollout keeps applying the command it applied
  last in the live world.

**Two experts in one bout** (the mirror).

- Both take their moment before either searches, so neither sees the plan the other is choosing on
  the same frame.
- In a rollout, the other corner plays its committed plan forward from its own restored shadows.
  `an_expert_opponent_is_played_forward_from_its_own_plan` holds this bit for bit.

**The expert's own state is not world state.** The world graph pairs its slot with an empty
`Forkable` (`ExpertShell`).

- Its shadows travel by `snapshotMind` / `restoreMind`. The shadows are the duelist and the walker,
  kept running beside it so that a plan can follow one.
- Its plan in progress travels by `Program.clone`.

**Fork reuse.**

- One fork world is kept and restored for every candidate. Havok's whole memory is copied over it
  and the JavaScript graph is rewritten, which is the same restore a fresh fork gets.
- It is rebuilt when the capture's topology differs from its own (a sever, a ruin or a grip), or
  when a rollout changed its own.
- `a_reused_fork_is_a_fresh_fork` holds that a reused fork chooses and predicts exactly what a fresh
  exact fork per candidate does.
- In the league gate it built 925 fork worlds for 21,944 rollouts.

**Timing.** A search forks Havok instances, which is asynchronous, and a mind's `decide` runs inside
the solver's step. So the search runs between frames:

- in a bout, `runExpertBout` awaits it before each frame;
- in a drill, the drill runner's planner hook does.

The drill runner builds a rung's planner with `rungFactory`, and keeps the planner's summary on the
rung's result.

**Mutation checks**, each run and each turned red:

- the shadows not restored into a rollout;
- a capture without Havok's heap (the teleport fork);
- a reused fork not restored;
- an expert opponent left to its fork slot's shell, or played without its shadows;
- the drill task judging the live world, never framed, or left off;
- the search keeping the worst plan;
- `-lag` searching with no stale capture, or on the live moment, or parsed without its wait.

Two mutations survive, and both are equivalent here:

- **not cloning a warm plan**, because the moment already holds a clone and only one rollout of a
  fresh moment plays it;
- **the drill host's range feed**, because no drill reads it inside a rung.

The runner's wall limit per job is now an option (`jobLimitMs`, `--job-minutes`): an expert bout
takes longer than five minutes. The limit is held by a test in `tests/research.test.mjs`, which was
mutation-checked.

## 2. Cost

Node bout runner, `default` against `default`, the expert against the duelist, one lane
(`.review/expert-cost.mjs`, 4 s of bout), at c8, h1, 4 Hz:

| per decision | ms |
|---|---:|
| capture with the heap | 37 |
| restore, per rollout | 47 |
| simulate 1 s, per rollout | 281 |
| a whole decision (8 rollouts), median | 2,690 |

- That is **11 s of wall a game second**, or about 1/11 of real time.
- The plan estimated 1/250 at 8 decisions a second over 32 one-second rollouts. At the measured
  328 to 385 ms a rollout, that configuration would run at 1/85 to 1/100.
- Under the load of these runs (13 to 20 lanes), the league gate read 3.08 s a decision, 12.6 s a
  game second and 385 ms a rollout: 345 ms of restore and 2,698 ms of simulation a decision.
- Simulation is 88 % of the cost. So the cost is linear in candidates × horizon, and the fork itself
  is not the bottleneck.

Drill runner, same host, per rung (s a decision / s of wall a game second):

| rung | survive-cut | punish-miss | land-clean-blow | hold-range | get-inside | finish |
|---|---|---|---|---|---|---|
| c4, h1 | 1.05 / 4.2 | 0.65 / 3.9 | 1.01 / 7.0 | 1.21 / 4.8 | 0.70 / 3.9 | 0.76 / 6.5 |
| c8, h1 | 1.96 / 7.8 | 1.41 / 8.4 | 1.80 / 12.7 | 2.35 / 9.4 | 1.31 / 7.3 | 1.41 / 13.9 |
| c16, h1 | 3.92 / 15.7 | 2.71 / 15.9 | 3.13 / 21.9 | 4.74 / 19.0 | 2.49 / 14.0 | 2.23 / 21.9 |
| c32, h1 | 7.79 / 31.1 | 5.07 / 29.6 | 6.10 / 41.8 | 9.03 / 36.1 | 6.18 / 28.4 | 4.03 / 40.4 |

A drill decision is cheaper than a bout's, because a rollout stops where the rung does or at its
pass.

## 3. Score against compute

Drill runner, `default` against `default`, the grid `research/runs/expert-drills-1` (200 runs a
drill), with each rung paired by start with every other. Pass rate, with the margin in brackets:

| rung | survive-cut (wound) | punish-miss (s) | land-clean-blow (s) |
|---|---|---|---|
| c4, h1 | 83.9 % (0.016) | 100 % (0.261) | 100 % (0.209) |
| c8, h1 | 86.0 % (0.013) | 100 % (0.263) | 100 % (0.206) |
| c16, h1 | 86.0 % (0.012) | 100 % (0.282) | 100 % (0.204) |
| c32, h1 | 88.2 % (0.015) | 100 % (0.286) | 100 % (0.212) |
| c8, h0.5 | 86.0 % (0.013) | 100 % (0.263) | 100 % (0.207) |
| c8, h1.5 | 86.0 % (0.013) | 100 % (0.263) | 100 % (0.206) |

- **Candidates: flat.** From c4 to c32 the pass rates are within 4.3 points on survive-cut and
  identical on the other two. The margins stay within 0.03 s of one another.
  - More candidates slightly *lengthen* punish-miss's time to a blow: c16 and c32 against c8 are
    −0.018 ± 0.011 and −0.022 ± 0.012 s.
  - The extra candidates find heavier blows a little later. The rollouts' predicted damage rises
    0.088 → 0.107 → 0.121 from c8 to c32, because the objective counts a blow's damage at full
    weight and its earliness at a quarter. On get-inside the same pull is much larger (below).
- **Horizon: flat, and partly by construction.**
  - survive-cut and punish-miss are one-second rungs, so a rollout of 1 s or 1.5 s is capped at
    what remains of the rung: h1.5 ≡ h1 there. On both drills h1.5 and h1 differ in 0 of 93 and 0
    of 200 runs.
  - On punish-miss, h0.5 also chose identically in every run, because each rollout stops at its
    pass, and the pass comes inside half a second.
  - On survive-cut, h0.5 differs from h1 in 19 of 93 runs, with the same pass rate.
- **Where the curve has not flattened: survive-cut's low line.** Only 15 of the grid's scored
  starts cut low, so this is a small sample.
  - The expert passes 20.0, 26.7, 33.3 and 33.3 % from c4 to c32.
  - The guardless duelist passes 40.0 % and the duelist 0 %.
  - Headroom here is a lower bound, and the proposals may simply lack the right cover.
    - No proposal covers a low line on purpose: every guarding stance covers the duelist's line
      (`coverLift`).
    - Jitter moves only the mark that the cover aims at.
- **By line**, at c8:

  | line | scored | duelist | expert |
  |---|---:|---:|---:|
  | high | 66 | 34.8 % | 97.0 % |
  | middle | 12 | 58.3 % | 100 % |
  | low | 15 | 0 % | 26.7 % |

**The three-second drills.** The same grid, 200 runs a drill. Pass rate, with the margin in
brackets:

| rung | hold-range (share in band) | get-inside (s inside) | finish (s to first blow) |
|---|---|---|---|
| duelist | 96.5 % (0.982) | 91.5 % (1.209) | 100 % (0.096) |
| walker | 0 % (0.132) | 100 % (0.327) | 99.5 % (0.753) |
| c4, h1 | 99.0 % (0.993) | 100 % (0.369) | 100 % (0.132) |
| c8, h1 | 99.5 % (0.996) | 100 % (0.386) | 100 % (0.103) |
| c16, h1 | 100 % (0.998) | 100 % (0.376) | 100 % (0.103) |
| c32, h1 | 100 % (0.999) | 100 % (0.839) | 100 % (0.100) |
| c8, h0.5 | 98.0 % (0.994) | 100 % (0.334) | 100 % (0.099) |
| c8, h1.5 | 100 % (0.997) | 100 % (0.401) | 100 % (0.103) |

The lighter grid that stood in for these while the big one ran (`research/runs/expert-drills-long`,
60 runs a drill on the same starts) agrees with them to within its own noise.

- **Candidates: flat from c8 on hold-range and finish.** finish's time to first blow is the one
  margin that moves, from 0.132 s at c4 to 0.103 at c8 (c4 against c8, −0.029 ± 0.019). After
  that it does not move (c32 against c8, +0.003 ± 0.007).
- **Get-inside at c32: more search, a worse drill margin.** c32 passes every start but gets
  inside at 0.839 s against c8's 0.386 (−0.453 ± 0.098 paired).
  - Per start, the median difference is 0.10 s. c32 is more than 0.3 s later in 80 of 200 starts,
    and more than 1.6 s later in a tenth of them.
  - The search is not failing, because its own objective rises with candidates. The mean
    predicted terms of a chosen plan are:

    | rung | total | damage | task | s inside |
    |---|---:|---:|---:|---:|
    | c4 | 1.260 | 0.042 | 1.218 | 0.369 |
    | c8 | 1.263 | 0.046 | 1.218 | 0.386 |
    | c16 | 1.268 | 0.050 | 1.218 | 0.376 |
    | c32 | 1.300 | 0.111 | 1.189 | 0.839 |

  - c32 gives up 0.029 of task term, which is a later entry, for 0.065 of predicted damage. It
    does that with back-off-then-cut plans: 102 of its 771 choices on the drill, 98 of them in the
    late starts. c8 and c16 chose one in 0 of 428 and 0 of 423.
  - They cannot choose one. Round 1 takes the first `candidates − ⌊candidates / 3⌋` plans of the
    fixed list (§1): 6 at c8, 11 at c16, and at c32 the whole list (15 plans, or 16 with a warm
    one) plus random ones. Back-off-then-cut is the last. So **the candidates axis buys proposal
    coverage as well as density**. At c8 the search never rolls out the walker, guard,
    cut-then-back-off, the two circles, cut-low, hold, duelist-then-cut or back-off-then-cut, nor
    press on a decision that carries a warm plan, except where a jitter of an elite happens to
    resemble one.
  - A drill's rollout is scored by the bout objective plus the drill's task. So on get-inside a
    blow of 0.1 bar is worth 1.2 s of lateness. That is a mis-specified objective, which a wider
    search exposes: it is the clearest case in this session of the objective and the drill
    measuring different things.
- **Horizon: a trade-off, not a budget.** A longer horizon is slower to get inside.
  - On get-inside, h0.5 is 0.052 ± 0.022 s ahead of h1 and within 0.007 ± 0.004 s of the walker.
    h1.5 is 0.074 ± 0.025 s behind the walker.
  - A longer rollout sees more of what follows the pass: the longer arm's next stroke, a heavier
    blow a moment later. The objective prices those at full weight and earliness at 0.25 / 3 bars
    a second.
  - So the horizon picks which question the expert answers. Session 05 should hold it fixed
    rather than treat it as compute.

## 4. The gate

### League

The harness is the Node bout runner through `research/league.mjs`, with the research `PROTOCOL` cap
and supported locomotion. The blade class (`default`) plays each of its four variants: plated,
multileg, wheel and ram. There are 8 clusters, so n = 128 bouts: 64 corner-swapped pairs in 32
clusters.

**Why n = 128.** It was set before the run, at session 03's walker mirror size, where the band is
about ±9 points. That is enough to separate a 60/40 mind from an even one at about 2 SE. The result
did not need it:

- 128 of 128 wins;
- the one-sided 95 % lower bound on the expert's win rate is 97.7 %;
- the paired margin's d is 8.37.

| A expert@c8,h1 vs B duelist | value |
|---|---|
| A score | 100.0 % [100.0, 100.0]; A left 100.0, A right 100.0 |
| bar margin | 0.833 [0.812, 0.853], d 8.37 |
| by variant | 32/32 on each of plated, multileg, wheel, ram |
| stall s a bout, A / B | 0.00 / 0.00 |
| retreat s a bout, A / B | 0.00 / 0.00 |
| lead changes a bout | 0.04 |
| winner's bar | 0.861 |
| decided before the cap | 100 % |
| falls a bout | 0.24 |
| length | 5.23 s |

What it chose, as a share of 2,743 decisions:

| plan | share |
|---|---:|
| the plan in progress, warm | 26.1 % |
| the duelist's continuation | 14.7 % |
| step-cut | 11.7 % |
| the duelist, jittered | 10.2 % |
| step-cut, jittered | 7.0 % |
| cut-high | 6.9 % |
| cut-mid | 6.4 % |
| the rest | 17.0 % |

It is not winning by a trick the duelist has never seen, such as standing off or stalling: the
guard columns are zero. It wins by choosing, a quarter-second at a time, when to follow the
duelist's own play and when to step in and cut.

### Drills

The drill runner, paired by start against the ladder: idle, walker and duelist, plus the guardless
duelist. The rows below are the grid at c8, h1. The expert beats the duelist:

- **survive-cut**: 86.0 against 32.3 % (+53.8 ± 10.6, n 93);
- **punish-miss**: 100 against 67.5 % (+32.5 ± 6.5, n 200). Its first blow lands at 0.26 s against
  the duelist's 0.50 s;
- **land-clean-blow**: 100 against 79.0 % (+21.0 ± 5.7, n 200). Its clean blow lands at 0.21 s
  against 0.55 s.

The walker, idle and the guardless duelist are below the duelist on each of these, except that the
walker ties it on punish-miss (+4.5 ± 9.2). So on these three drills the expert beats every rung of
the ladder.

The three-second drills (the same grid, n = 200 each):

- **get-inside.** The expert beats the duelist by 8.5 ± 3.9 points and by 0.82 ± 0.09 s inside.
  - It does not beat the walker, which walks straight in at 0.33 s against the expert's 0.39
    (−0.059 ± 0.022).
  - The duelist fails this drill by its stand-off rule (session 03). The walker is the ladder's
    best rung here, and the expert matches its pass rate without matching its speed.
- **hold-range.** 99.5 against 96.5 % (+3.0 ± 2.7). The expert's share in band is 0.996 against
  the duelist's 0.982 (+0.014 ± 0.004). The walker cannot pass it by construction.
- **finish.** Saturated on pass rate, and level on time to first blow: 0.103 against 0.096 s
  (−0.007 ± 0.007). The duelist already strikes a downed body's live core at once
  (`finishPoint`). The expert's first candidate *is* the duelist's continuation, and it leaves it
  only for a plan it scores higher: a heavier blow a moment later.

**Where the gate is thin.** On get-inside's time and finish's time, what the expert maximises and
what the drill's margin measures are not the same quantity. Its task term prices the pass at 1 and a
second of earliness at 0.25 / 3 bars, so a heavier or safer blow 50 ms later outscores a lighter one
now. That is a property of the objective and not a fault in the fork or the search:
- the shorter horizon (h0.5) closes the gap to the walker;
- the lighter c4 search is the slower one on finish;
- the wider c32 search is 0.45 s *slower* on get-inside than c8, while scoring higher on its own
  objective (§3). That is the plainest evidence that the objective, and not the search, is what
  separates the expert from the drill's margin.

If session 05 needs these margins as headroom, the task term should weight time more heavily, or
a drill's rollout should drop the bout's damage term. That is an owner decision, because it
changes what "better" means in a drill.

## 5. The sanity checks

### Beats idle on every body

League, all seven weapon classes, each against its variants (27 pairs; ram + ram omitted), 1
cluster, n = 108:

| class | bouts | expert score | margin | length |
|---|---:|---:|---:|---:|
| blade | 16 | 100 % | 0.999 | 5.3 s |
| two-blades | 16 | 100 % | 0.982 | 4.1 s |
| mace | 16 | 100 % | 0.929 | 8.9 s |
| maul | 16 | 100 % | 0.786 | 13.4 s |
| whip | 16 | 100 % | 0.970 | 33.6 s |
| fist | 16 | 100 % | 0.817 | 25.9 s |
| ram | 12 | 100 % | 0.395 | 83.9 s |

- Every bout was decided before the cap, on both sides.
- The ram class takes longest, and keeps least of the expert's bar. A ram against a standing body is
  a slow weapon, not a failed search: its margin interval is [0.166, 0.666], wholly above 0.

### The mirror

League, `--mirror expert@c8,h1`, 2 clusters each on the blade and two-blades classes. Session 01's
gate is read over distinct bouts (`sideVerdict` in `research/side-mirror.mjs`):

| mirror | bouts | distinct | left corner | band | verdict | winner's bar | lead changes | length |
|---|---:|---:|---:|---:|---|---:|---:|---:|
| blade | 32 | 32 | 50.0 % | ±17.3 | inside | 0.361 | 1.59 | 13.8 s |
| two-blades | 32 | 32 | 46.9 % | ±17.3 | inside | 0.326 | 2.03 | 7.8 s |
| pooled | 64 | 64 | 48.4 % | ±12.3 | inside | | | |

- Every bout was decided. The winners kept about a third of their bars, with 1.6 to 2 changes of
  lead a bout. These were real fights, not one side walking through.
- Blade is 4 variants × 2 clusters × 4 bouts; two-blades is the same shape on the two-blades class.
- The bands are wide at this n. What they rule out is a side that decides the mirror, as the
  guardian's does (`SIDE_DECIDED`).

### A blinded fork

The plan's mutation is "a fork that restores a stale state". The drill grid ran it as `-blind`: four
decisions stale, and, before four decisions have passed, the oldest capture held. **In a one-second
rung that means the first decision is exact and each later one plans on the start again.**

| rung | survive-cut | punish-miss | land-clean-blow (s to clean blow) | hold-range (share) | get-inside (s inside) | finish (s) |
|---|---|---|---|---|---|---|
| full (c8, h1) | 86.0 % | 100 % | 100 % (0.206) | 99.5 % (0.996) | 100 % (0.386) | 100 % (0.103) |
| `-blind` (stale 4) | 83.9 % (−2.2 ± 4.2) | 59.0 % (−41.0 ± 6.8) | 97.0 % (0.522) | 97.5 % (0.984) | 99.0 % (0.391) | 100 % (0.102) |
| `-lag` (stale 1, no search before it) | 29.0 % (−57.0 ± 10.6) | 77.0 % (−23.0 ± 5.8) | 100 % (0.316) | 98.5 % (0.993) | 98.5 % (0.484) | 100 % (0.098) |

Paired against full knowledge on the same starts, n 93 on survive-cut and 200 elsewhere.

- `-blind` is level with full knowledge on survive-cut, the drill whose outcome is settled by the
  first decision. That decision is exact under `-blind`, and every later one re-chooses the same
  parry from the same start.
- `-lag` was added for that reason. It is the lightest stale fork there is: one decision (0.25 s)
  old, and no search at all until a capture that old exists.
  - On survive-cut it is the duelist (−3.2 ± 7.0 against the duelist), because its first quarter
    second *is* the duelist's.
  - On the three-second drills (n = 200):
    - get-inside: 98.5 % and 0.484 s inside, 0.098 ± 0.015 s behind full knowledge;
    - hold-range: level (98.5 %, 0.993; −0.003 ± 0.003);
    - finish: it never searched. The pass comes at 0.1 s, before its first search at 0.25 s, so it
      is the duelist there by construction.
  - `-blind` on the three-second drills is a little behind on hold-range's share (−0.013 ± 0.005)
    and level on the other two.
  - So it is measurably worse than full knowledge wherever it gets to search before the drill is
    decided.
- The lag rung was run on the grid's starts in a directory of its own
  (`research/runs/expert-drills-lag`) and joined by job id. Its duelist rung is identical to the
  grid's in every one of the joined runs, which confirms that the starts are the same.

The lesson is about the mutation. A stale fork whose staleness starts after the drill has been
decided detects nothing, and a green check of it would have been a detector that could not fire.

## 6. Model-only beside full knowledge

The persistence model in the opponent's place, everything else equal:

| | full knowledge | persistence | duelist |
|---|---:|---:|---:|
| survive-cut, drill runner, n 93 | 86.0 % | 50.5 % | 32.3 % |
| punish-miss, drill runner, n 200 | 100 % (0.263 s) | 100 % (0.281 s) | 67.5 % |
| land-clean-blow, drill runner, n 200 | 100 % (0.206 s) | 100 % (0.204 s) | 79.0 % |
| get-inside, drill runner, n 200 | 100 % (0.386 s) | 95.5 % (0.415 s) | 91.5 % (1.209 s) |
| hold-range, drill runner, n 200 | 99.5 % (0.996) | 93.5 % (0.982) | 96.5 % (0.982) |
| finish, drill runner, n 200 | 100 % (0.103 s) | 100 % (0.097 s) | 100 % (0.096 s) |
| league vs duelist, blade | 100 %, n 128, d 8.37 | 85.9 % [78.1, 93.8], n 64, d 1.51 | |

- **Survive-cut is reading.** A parry has to be chosen before the cut shows, and only the
  full-knowledge rollout sees the cut coming. Persistence still beats the duelist by 18.3 ± 11.6.
- **The other five drills are search.** On punish-miss, land-clean-blow, get-inside and finish,
  persistence is within a few points and a few hundredths of a second of full knowledge. It passes
  get-inside a little less often (95.5 against 100 %, −4.5 ± 2.9) and holds range a little less
  often (93.5 against 99.5 %, −6.0 ± 3.3), both n 200. The opponent's next second changes little
  that matters there.
- **In the league, the gap is 14 points of score and more than half the margin** (0.833 against
  0.389).
  Model-only's A-left and A-right are 93.8 and 78.1 %. That is within the noise at 32 bouts a side,
  but it should be watched.
- So the plan's warning applies, and is measured here. Full knowledge against the duelist is close
  to perfect prediction, because the duelist barely draws dice. Part of its margin is reading this
  one opponent, and a body's headroom read by it carries that part.

## 7. What this leaves for session 05

- **The ruler.** `expert@c8,h1` at 4 Hz: flat against compute on the drills measured, at 11 to
  13 s of wall a game second.
  - A league cell of 128 bouts against the duelist costs about 2.3 lane-hours, and longer bouts
    cost proportionally more.
  - One c8 rung of a drill costs 0.4 lane-hours for 200 one-second runs. A three-second drill
    costs between 0.2 lane-hours (get-inside, whose runs end at the pass) and 1.6 (hold-range,
    which runs all three seconds).
- **Candidates are proposal coverage too.** At c8 the search rolls out only the first six of the
  fixed plans. A curve that is flat from c8 says that the other plans do not change a pass. It
  does not say they were never better. c32's back-off-then-cut on get-inside is one that the
  objective prefers and the drill does not. If session 05 wants density without a change of
  families, the fixed list should be sized to fit round 1, or ordered per drill.
- **Read the persistence expert beside it.** A body whose headroom shows only under full knowledge
  is being read through the duelist's predictability.
- **Survive-cut's low line** is the one place where more search still paid, from 20 % at c4 to 33 %
  at c32. It is worth a closer look before it is called a body's ceiling. A proposal that covers a
  low line on purpose is the first thing to try.
- **Hold the horizon fixed.** On the three-second drills it trades earliness against what follows,
  so a change of horizon changes the question as well as the budget.
- **Decisions for the owner:**
  - **Is `expert@c8,h1` at 4 Hz the ruler?** It is flat against candidates. It costs 12× real time.
  - **Full knowledge or persistence as the ruler, or both side by side.** Full knowledge reads the
    duelist almost perfectly, and persistence is closer to "a fact about the body".
  - **Should the drill task weight time more heavily, or drop the bout's damage term inside a
    drill?** As it stands the expert does not beat the walker on get-inside's time, nor the
    duelist on finish's, and a wider search gets further from the drill's margin: c32 is 0.45 s
    slower to get inside than c8.
  - **Which mutation stands as the blinded-fork sanity check.** `-lag` bites everywhere the
    expert searches before the drill is decided. `-blind` (stale 4) is inert on survive-cut.

## Runs

Every directory is under `research/runs/` (gitignored), with the x1 stone `default` build against
itself unless a league class says otherwise.

| directory | harness | what | n |
|---|---|---|---|
| `expert-drills-1` | drill runner | ladder + 9 expert rungs, 6 drills | 200 a drill |
| `expert-drills-lag` | drill runner | ladder + `-lag`, joined to the grid by job id | 200 a drill |
| `expert-drills-long` | drill runner | ladder + 7 expert rungs, the three-second drills | 60 a drill (superseded, agrees) |
| `expert-league-c8` | league | expert vs duelist, blade | 128 |
| `expert-persist-league-c8` | league | persistence expert vs duelist, blade | 64 |
| `expert-idle-c8` | league | expert vs idle, all classes | 108 |
| `expert-mirror-c8` | league | expert mirror, blade | 32 |
| `expert-mirror-c8-two` | league | expert mirror, two-blades | 32 |

The big grid ran from 22:21 to 04:41 and finished all 1,200 runs with none failed. It was started
after main's get-inside size fix (c8d38fc) was merged, so its get-inside is the corrected drill.
While it ran, the lighter `expert-drills-long` (60 runs a drill, without c32, `-blind` or `-bout`)
stood in for its three-second drills. That grid ran on the same starts, because a seed depends
only on the drill and the run, and it agrees within its noise. Every figure above is the big
grid's.
