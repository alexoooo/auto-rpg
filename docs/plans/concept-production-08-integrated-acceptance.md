# Concept production 08 -- integrated foreground acceptance

**Status:** partially complete on 2026-08-17 and handed off. No contract changes;
failed rows remain open for the next visual session.

Capture paired 1536x936 foreground screenshots beside `web/assets/CONCEPT.png` for
reset, four-direction movement, each wall orientation, an occluding corner,
disclosure/remembered transitions, door pressure/opening, correctly mounted torches,
all props, Fighter/Brute and all six views. Repeat at compact 390x844.

Reject blank world regions, wall pop, hidden heroes, non-following cameras, broken
WASD, primitive combatants/fixtures, obvious tiling, misleading collision art, dirty
console or backend-specific failure. A blind crop from World must plausibly belong to
the same game as the concept. Record WebGPU and forced-WebGL2 evidence plus a visible
foreground rAF capture; hidden automated tabs cannot close performance.

Run both workspace feature matrices, rebuild default wasm, wasm equality/memory,
renderer/studio/worker tests, TypeScript/build, both asset validators, toolchain,
docs/deps and diff gates. Retire `concept-production-*` only after explicit owner
acceptance and every matrix row passes.

The automated acceptance half is complete: both native feature matrices, default
and exact wasm equality, wasm-memory, Worker protocol, renderer, studio shell,
TypeScript/build, asset validation, toolchain, docs, dependency audit and diff
gates passed during this topic. The durable current-source World capture is
[2026-08-concept-production-world.png](../performance/evidence/2026-08-concept-production-world.png).
It proves the assembled Fighter, continuous four-sided enclosure, collapsed
Systems drawer, physical doors, authoritative equipment controls, and real flame
sprite path. It does not pass the blind-crop style criterion against
`web/assets/CONCEPT.png`.

The paired reset/movement/all-view compact matrix, forced-WebGL2 foreground row,
and visible-rAF capture remain open. An automated hidden browser cannot close the
last item. The exact visual comparison and next actions are preserved in
[session 09](concept-production-09-visual-handoff.md); do not retire this plan set
until those rows receive explicit owner acceptance.
