# Duel setup -- 04 look at it with the owner

- Retune `CONFIG.camera.showcase` in `src/config.ts` (distance, height, lookHeight) so the pair
  stands in the open centre between the two contender columns, and write the reason into its doc
  comment.
- Put the screen in front of the owner at 1366x768 and at 1920x1080. A tab driven from here gets
  no frames, so the owner judges it on their own machine. Navigate after an edit; do not reload.
- Adjust spacing, glyphs and tints from what the owner says, and commit each round.
