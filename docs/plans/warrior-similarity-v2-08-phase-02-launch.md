# Session 08 -- launch experiment phase 02

## Outcome

Freeze the calibrated ruler and production-valid asset baseline, then complete
the first residual-led phase-02 experiment through snapshot, review, decision,
audit, and a synthetic archive rehearsal.

## Implementation

- Record formula-v2 annotations, component scales/weights, model hashes,
  reference consistency/confidence, acceptance margin, and stopping band.
- Promote the preferred authored/material asset to the phase-02 baseline only
  after explicit human acceptance; preserve phase 01 as a comparison artifact.
- Select the largest remaining coherent atlas residual whose required
  representation exists. Preregister affected cells, expected movement,
  unaffected spill, contacts, and production vocabulary.
- Capture and decide one complete experiment. Formal IDs are used for evidence,
  not modeling scratch work.
- Run the archive command against a synthetic fixture to prove the next phase
  can close without hundreds of tracked or ignored files.

## Verification

Run `npm test`, `npm run asset:validate`, `npm run build`,
`npm run similarity:test`, both similarity formulas, the residual atlas,
experiment audit, and a visible eight-view comparison. No development server or
generated review tree remains after the session.

